query GetRecentOrders($first: Int!) {
  orders(first: $first, sortKey: CREATED_AT, reverse: true) {
    edges {
      node {
        id
        name
        createdAt
        customer {
          id
          email
          tags
        }
        lineItems(first: 50) {
          edges {
            node {
              id
              sku
              name
              variant {
                id
                sku
                product {
                  id
                }
              }
            }
          }
        }
      }
    }
  }
}
