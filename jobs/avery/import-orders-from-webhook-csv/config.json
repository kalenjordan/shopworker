{
  "name": "Import from Webhook CSV",
  "trigger": "webhook",
  "shop": "kalen-test-store",
  "test": {
    "limit": 3,
    "dryRun": false,
    "webhookPayload": "fixtures/may-31-orders.json"
  }
}
